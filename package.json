{
  "name": "omarchy-kids-game-turbo-tables",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "preinstall": "node -e \"console.error('Do not install packages inside the plugin checkout: node_modules symlinks fail omarchy plugin validate. Tools run through npx; see docs/environment.md.'); process.exit(1)\"",
    "build": "node esbuild.config.mjs",
    "check:types": "npx --yes --package=typescript@7.0.2 tsc --noEmit",
    "check:boundary": "node src/tools/check-boundary.ts",
    "check:bundle": "node src/tools/verify-bundle.ts",
    "check:sprites": "node src/tools/check-sprites.ts",
    "sprites": "node src/tools/bake-sprites.ts",
    "check:readme": "node src/tools/check-readme.ts",
    "scan": "node src/tools/scan.ts",
    "vectors": "node src/tools/vectors.ts",
    "test": "node --test \"tests/**/*.test.ts\"",
    "check": "npm test && npm run check:types && npm run check:boundary && npm run check:bundle && npm run check:sprites && npm run check:props && npm run check:readme && npm run scan",
    "props": "node src/tools/bake-props.ts",
    "check:props": "node src/tools/check-props.ts"
  }
}
